export default (state = {
    isSending: false,
    image: null,
    images: [],
}, action) => {
    switch (action.type) {
        // case value:

        //     break;

        default:
            return state
    }
}